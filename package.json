{
  "name": "mcp-health-check",
  "version": "1.0.0",
  "description": "CLI utility to health-check MCP servers via stdio",
  "main": "dist/index.js",
  "bin": {
    "mcp-health-check": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "keywords": ["mcp", "health-check", "cli", "typescript", "ai-devtools", "mcp-server"],
  "author": "Launchery",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
